# Backend

## Installation

Install dependecies using

´´´
pip install -r requirements.txt
´´´

## Configuración
1. Crea un archivo `.env` en esta carpeta.
2. Agrega tus credenciales de Supabase:
   SUPABASE_URL=tu_url
   SUPABASE_KEY=tu_key_service_role
